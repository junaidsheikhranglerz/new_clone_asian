@model List<ranglerz_project.Models.Transaction>
@{
    ViewBag.Title = "salesReportByItemPost";
    var sesion = Session["name"].ToString();
    int result1;
    int result2;
    float clientWeight = 0;
    float originalweight = 0;
    float difference = 0;
    
    int salesReportsEdit = Convert.ToInt32(Session["sale_reports_edit"]);
  

}











    <div class="container body">


        <div class="main_container">

            @{
                Html.RenderPartial("_Partial");
            }

            <!-- top navigation -->
            <!-- /top navigation -->
            <!-- page content -->
            <div class="right_col col-md-10 pull-right" role="main">
                <div class="">
                    <div class="page-title">
                        <div class="title_left">
                            <h3>
                                Sale Reports By Items
                            </h3>
                        </div>


                    </div>
                    <div class="clearfix"></div>
                    <div class="row">
                        <div class="x_content">
                            <br />
                            <form id="demo-form2" action="@Url.Action("salesReportByItemPost", "SaleVoucher")" method="post" data-parsley-validate class="form-horizontal form-label-left">

                                <div class="form-group">

                                    <div class="col-md-6 col-sm-6 col-xs-12">
                                        <select name="search" required class="form-control chzn-select col-md-7 col-xs-12">
                                            <option value="">Please select any </option>
                                            @foreach (var tr in ViewBag.mylist)
                                            {
                                                <option value="@tr.good_Name"> @tr.good_Name </option>
                                            }


                                        </select>
                                        <input type="date" id="dateDefault" name="dateStart" required />
                                        <input type="date" id="dateDefault2" name="dateEnd" required />
                                    </div>
                                    <div class="col-md-6 col-sm-6 col-xs-12">

                                        <input type="submit" value="Search" class="btn btn-success" />
                                    </div>
                                </div>
                                <div class="ln_solid"></div>


                            </form>




                        </div>

                    </div>

                    <div class="row">

                        <div class="col-md-12 col-sm-12 col-xs-12">
                            <div class="x_panel">
                                <div class="x_title">
                                    <h2>View Sale Reports</h2>
                                    <div class="clearfix"></div>
                                </div>
                                <div class="x_content">
                                    <table id="example" class="table table-striped responsive-utilities jambo_table">
                                        <thead>
                                            <tr class="headings">

                                                @if (sesion == "Admin")
                                                {
                                                    <th>User Name</th>
                                                }

                                                <th>Date </th>
                                                <th>Type </th>
                                                <th>Code </th>
                                                <th>Description </th>


                                                <th>Truck </th>

                                                <th>Item</th>
                                                <th>Client's Weight</th>
                                                <th>Factory Weight</th>
                                                <th>Action</th>

                                            </tr>
                                        </thead>

                                        <tbody>

                                            @foreach (var t in Model.Where(x => x.is_active == "Y"))
                                            {

                                                if (t.extra == "Sale")
                                                {
                                                    continue;
                                                }
                                                if (t.Id % 2 == 0)
                                                {
                                                    continue;
                                                }

                                                if (t.is_active != "Y")
                                                {
                                                    continue;
                                                }

                                                if (t.voucher_type == "JV" || t.voucher_type == "BV" || t.voucher_type == "BP" || t.voucher_type == "CP" || t.voucher_type == "CR" || t.voucher_type == "PV" || t.voucher_type == "UPPV")
                                                {
                                                    continue;
                                                }

                                                result1 = DateTime.Compare(ViewBag.start, t.created_at);
                                                result2 = DateTime.Compare(ViewBag.end, t.created_at);


                                                if (result1 < 0 || result1 == 0)
                                                {

                                                    if (result2 != -1)
                                                    {

                                                        <tr class="even pointer">

                                                            @if (sesion == "Admin")
                                                            {
                                                                if (t.User != null)
                                                                {
                                                                    <td>@t.User.name</td>
                                                                }
                                                                else if (@t.Admin != null)
                                                                {
                                                                    <td>@t.Admin.username</td>
                                                                }
                                                                else
                                                                {
                                                                    <td>N/A</td>
                                                                }
                                                            }
                                                            <td>@t.created_at.ToShortDateString()</td>




                                                            <td>@t.voucher_type</td>
                                                            <td>@t.voucher_code</td>
                                                            <td>@t.description <strong> From :</strong>  @t.from_account <strong>To :</strong>   @t.to_account </td>

                                                            <td>@t.truck</td>
                                                            @{
                                                            var tt = Convert.ToInt64(t.balance);
                                                            var balance = String.Format("{0:#,##0.00}", tt);
                                                            }

                                                            <td> @t.good_</td>
                                                            <td>@t.client_s_weight</td>
                                                            <td>@t.quantity</td>
                                                            @{
                                                            if (t.client_s_weight != null)
                                                            {
                                                                clientWeight = (float)(clientWeight + t.client_s_weight);
                                                            }
                                                            originalweight = (float)(originalweight + t.net_weight);
                                                            }
                                                            <td>
                                                                @if (salesReportsEdit == 1)
                                                                {
                                                                    <a href="@Url.Action("Edit","SaleVoucher" , new {id= @t.Id} )">Edit</a>
                                                                }
                                                            </td>





                                                        </tr>
                                                    }
                                                }
                                            }




                                        </tbody>

                                    </table>
                                    @{
                                        difference = originalweight - clientWeight;
                                    }
                                    <p><strong>Client Weight :</strong>  @clientWeight</p>
                                    <p><strong>Total Net Weight :</strong> @originalweight</p>
                                    <p><strong>Difference :</strong> @difference</p>
                                </div>
                            </div>
                        </div>

                        <br />
                        <br />
                        <br />

                    </div>
                </div>

            </div>

            <!-- /page content -->
        </div>

    </div>
    <script src="~/Scripts/js/bootstrap.min.js"></script>

    <script src="~/Scripts/js/custom.js"></script>


    <script>
    function setInputDate(_id) {
        var _dat = document.querySelector(_id);
        var hoy = new Date(),
            d = hoy.getDate(),
            m = hoy.getMonth() + 1,
            y = hoy.getFullYear(),
            data;

        if (d < 10) {
            d = "0" + d;
        };
        if (m < 10) {
            m = "0" + m;
        };

        data = y + "-" + m + "-" + d;
        console.log(data);
        _dat.value = data;
    };

    setInputDate("#dateDefault");
    </script>
    <script>
        function setInputDate(_id) {
            var _dat = document.querySelector(_id);
            var hoy = new Date(),
                d = hoy.getDate(),
                m = hoy.getMonth() + 1,
                y = hoy.getFullYear(),
                data;

            if (d < 10) {
                d = "0" + d;
            };
            if (m < 10) {
                m = "0" + m;
            };

            data = y + "-" + m + "-" + d;
            console.log(data);
            _dat.value = data;
        };

        setInputDate("#dateDefault2");
    </script>
















    <script type="text/javascript">
    function confirm_delete() {
        return confirm('are you sure?');
    }
    </script>

















